name: Publish THINKCAR V7.1

# Archived legacy publisher. Automatic execution is disabled.
# Kept as manual-only so the workflow can be deliberately rebuilt/re-enabled if needed.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived publisher
        run: |
          echo "Legacy THINKCAR V7.1 publisher is archived and automatic execution is disabled."
